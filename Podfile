# Global settings
platform :ios, '11.0'
use_frameworks!
inhibit_all_warnings!

target 'TMDb Challenge' do
    pod 'PromiseKit', '~> 6.3'
    pod 'Alamofire', '~> 4.7'
    pod 'Kingfisher', '~> 4.8'

    target 'TMDb ChallengeTests' do
      inherit! :search_paths
      # Pods for testing
    end

end
